const operación = (a, b, funcion) => funcion(a, b);
const suma = (a, b) => a + b;
const resta = (a, b) => a - b;
const producto = (a, b) => a * b;
const dividir = (a, b) => a / b;
const modulo = (a, b) => a % b;

//PROMESAS

console.log(dividir(10, 2));
